# Single n8n Webhook URL for all operations
# The application uses a single entry point and distinguishes actions via the '?action=' query parameter.
NEXT_PUBLIC_N8N_API_URL=https://your-n8n-instance.com/webhook/api
